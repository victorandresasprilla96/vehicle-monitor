# Traccar server the dev proxy (and Vercel rewrites) forward /api to
VITE_TRACCAR_TARGET=https://demo4.traccar.org
# Base path the app calls — always the same-origin proxy
VITE_API_BASE=/api
# Position polling interval in milliseconds
VITE_POLL_INTERVAL_MS=5000
